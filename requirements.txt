# torch: CPU version installed by default.
# For GPU support, after running setup.sh install the correct variant:
#   CUDA 11.8: pip install torch --index-url https://download.pytorch.org/whl/cu118
#   CUDA 12.1: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=1.13.0
transformers>=4.30.0
numpy>=1.21.0
pandas>=1.5.0
scipy>=1.9.0
tqdm>=4.62.0
